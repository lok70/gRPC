services:
  grpc-auth:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "44044:44044"
    volumes:
      - grpc_auth_data:/data
    restart: unless-stopped

volumes:
  grpc_auth_data:
